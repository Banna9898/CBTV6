(function(){ function applyTheme(t){ if(t) document.documentElement.setAttribute('data-theme', t); else document.documentElement.removeAttribute('data-theme'); localStorage.setItem('cbt_theme', t || ''); } document.querySelectorAll('.theme-set').forEach(btn=>{ btn.addEventListener('click', e=>{ e.preventDefault(); applyTheme(btn.dataset.theme); }); }); const saved = localStorage.getItem('cbt_theme') || ''; if(saved) applyTheme(saved); })();
