services:
  - type: web
    name: cbt-v6-creative
    env: docker
    plan: free
    buildCommand: docker build -t cbt-v6-creative .
    startCommand: docker run -p 10000:80 cbt-v6-creative
    autoDeploy: true
